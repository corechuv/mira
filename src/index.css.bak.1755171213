@tailwind base;
@tailwind components;
@tailwind utilities;

:root { color-scheme: light; }

.container-narrow { @apply container mx-auto max-w-6xl px-4 md:px-6; }

.btn { @apply inline-flex items-center justify-center gap-2 rounded-2xl px-4 py-2 text-sm font-medium transition active:scale-[.98]; }
.btn-primary { @apply btn bg-brand-600 text-white hover:bg-brand-700; }
.btn-outline { @apply btn border border-slate-300 hover:bg-slate-50; }

.input { @apply w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm outline-none ring-0 focus:border-brand-500; }
.card { @apply rounded-2xl border border-slate-200 bg-white shadow-sm; }
.card-body { @apply p-4; }
.badge { @apply inline-flex items-center rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium; }

/* skeleton / shimmer */
.skeleton { @apply relative overflow-hidden bg-slate-100; }
.skeleton::after {
  content: "";
  @apply absolute inset-0 -translate-x-full;
  background: linear-gradient(90deg, rgba(255,255,255,0) 0%, rgba(255,255,255,.6) 50%, rgba(255,255,255,0) 100%);
  animation: shimmer 1.2s infinite;
}
@keyframes shimmer { 100% { transform: translateX(100%);} }

/* rating stars */
.stars { @apply text-yellow-500; }
.stars .dim { @apply text-slate-300; }

/* ——— layout utils ——— */
html.no-scroll, body.no-scroll { overflow: hidden; }
.safe-top { padding-top: env(safe-area-inset-top); }

@layer components {
  .btn{ @apply inline-flex items-center justify-center rounded-pill bg-black text-white px-4 py-2 text-sm font-medium transition hover:bg-black/90 active:scale-[.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-black/50 disabled:opacity-50 disabled:pointer-events-none; }
  .btn-outline{ @apply inline-flex items-center justify-center rounded-pill border border-black text-black px-4 py-2 text-sm font-medium transition hover:bg-black hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-black/50; }
  .btn-ghost{ @apply inline-flex items-center justify-center rounded-pill px-3 py-2 text-sm text-slate-900 hover:bg-black/5 transition; }
  .field{ @apply rounded-pill border border-black/15 bg-white px-4 py-2 text-sm placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-black/50; }
  .card{ @apply rounded-2xl border border-black/10 bg-white shadow-soft; }
  .container-narrow{ @apply mx-auto w-full max-w-6xl px-4; }
}

/* ——— reviews/textarea shape fix ——— */
@layer components {
  textarea.field{ @apply rounded-2xl min-h-[120px] resize-y; }
}
